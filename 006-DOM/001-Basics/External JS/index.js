console.log(window);
// NodeJS -> Reference Error: window is not defined
// We can use window only in browser

window.document.write("Hello From JS");
// This will write Hello from JS on web page (we have manipulated the HTML of website)